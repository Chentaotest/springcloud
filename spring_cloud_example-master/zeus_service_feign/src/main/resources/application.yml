server:
  port: 8765     #指定服务端口

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: service-feign  #服务名

#打开熔断器
feign:
  hystrix:
    enabled: true